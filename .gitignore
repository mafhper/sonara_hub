# build e dependências
node_modules/
dist/
site/dist/

# artefatos locais de execução (ver .dev/README.md → "Caminhos de runtime")
outputs/
input/
.dev/
test-results/
.worktrees/

# estado local do app (presets, histórico de jobs, preferências).
# Ignora o diretório inteiro de propósito: arquivos .tmp de escrita atômica
# (server/job-store.mjs grava em <arquivo>.tmp e renomeia) escapariam de um
# padrão por nome e apareceriam como não rastreados.
data/

.env
AGENTS.md
docs/

# sistema operacional / editores
.DS_Store
Thumbs.db
Desktop.ini
*.swp
